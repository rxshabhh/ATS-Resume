# Lower bounds only. After a successful install (see README), run
#   pip freeze > requirements.lock.txt
# to capture the exact resolved versions for reproducible setups.

# --- web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12       # required for multipart/form-data uploads

# --- configuration ---
pydantic-settings>=2.6
python-dotenv>=1.0             # backs pydantic-settings' env_file support

# --- database ---
sqlalchemy>=2.0
alembic>=1.14
psycopg2-binary>=2.9

# --- document parsing ---
pdfplumber>=0.11
python-docx>=1.1               # used by services/parser.py for .docx resumes

# --- keyword extraction ---
# The en_core_web_sm model is a separate download; see README.
spacy>=3.7

# --- LLM ---
google-genai>=1.0

# --- caching (optional at runtime; the app degrades to no-cache) ---
redis>=5.0

# --- tests ---
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27                    # required by fastapi.testclient
